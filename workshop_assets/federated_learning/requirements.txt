# Pinned workshop runtime baseline.
# Update deliberately, validate on all workshop runtime images, then record
# the resulting edge-ai-blueprint commit in the workshop release record.
flwr==1.36.0
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.1
cryptography==46.0.7
