apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: jupyterhub-redirect-slash
  namespace: {{ tier1.digitafrica_namespace }}
spec:
  redirectRegex:
    regex: ^(https?://[^/]+)/jupyter$
    replacement: ${1}/jupyter/
    permanent: false
