---
# Full deployment entry point — runs tier0 then tier1.
# For targeted runs use the individual playbooks:
#   ansible-playbook playbooks/tier0.yml
#   ansible-playbook playbooks/tier1.yml

- import_playbook: tier0.yml
- import_playbook: tier1.yml
